name: Recipe shards

on:
  push:
    branches: [main]
    paths:
      - "alpha/**"
      - "alpha_catalog/**"
      - "claude_index/**"
      - "recipe_detail/**"
      - "pantry_data/shard_hay_index.json"
      - "scripts/**"
      - "index.html"
      - "pantry.html"
      - "sw.js"
      - ".github/workflows/recipe-shards.yml"
  pull_request:
    branches: [main]
    paths:
      - "alpha/**"
      - "alpha_catalog/**"
      - "claude_index/**"
      - "recipe_detail/**"
      - "pantry_data/shard_hay_index.json"
      - "scripts/**"
      - "index.html"
      - "pantry.html"
      - "sw.js"
      - ".github/workflows/recipe-shards.yml"

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Verify index + detail alignment
        run: python3 scripts/check-recipe-shards.py
